var omdb = require('./');

omdb.search('the simpsons', function (err, shows) {
    var i;

    for (i = 0; i < shows.length; i += 1) {
        if (shows[i].type === 'series') {
            console.log(shows[i].imdb);
            break;
        }
    }
});
